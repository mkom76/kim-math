# Leave VITE_API_BASE_URL unset in development.
# src/api/client.ts derives the API host from the current frontend host
# (localhost vs 127.0.0.1) so browser dev and Capacitor dev keep cookies same-site.
