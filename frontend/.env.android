# Production API used by the packaged Capacitor Android app.
# Keep the web production build on the same-origin `/api` value in
# `.env.production`; the native shell has a localhost origin and therefore
# needs the absolute public API URL.
VITE_API_BASE_URL=https://kim-math.site/api
VITE_PUSH_ENABLED=true
